{
  "name": "basic-webdev-setup",
  "private": true,
  "version": "0.0.1",
  "description": "Entwicklungsumgebung basierend auf Vite, JS, SCSS",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vite test",
    "coverage": "vite test run --coverage",
    "format": "prettier --write \"src/**/*.js\" \"src/**/*.html\" \"src/**/*.scss\""
  },
  "devDependencies": {
    "sass": "^1.58.0",
    "vite": "^4.0.0"
  }
}
